# H??ng d?n Ch?nh s?a Nhân viên - Employee Edit Feature

## ? T?ng quan

Tính n?ng **Ch?nh s?a nhân viên** cho phép c?p nh?t ??y ?? thông tin nhân viên bao g?m:
- ? H? tên
- ? Ch?c v?
- ? Gi?i tính
- ? Ngày sinh
- ? S? ?i?n tho?i
- ? Ngày vào làm
- ? ??a ch?
- ? Tr?ng thái làm vi?c

---

## ?? Cách s? d?ng

### B??c 1: Xem chi ti?t nhân viên
1. Truy c?p: `/Admin/VehicleShift/EmployeeList`
2. Click button **"Xem chi ti?t"** trên card nhân viên
3. Modal hi?n th? thông tin ??y ??

### B??c 2: M? form ch?nh s?a
1. Trong modal chi ti?t, click button **"Ch?nh s?a"**
2. Modal chi ti?t ?óng l?i
3. Modal **"Ch?nh s?a thông tin nhân viên"** hi?n th?
4. Form t? ??ng ?i?n s?n d? li?u hi?n t?i

### B??c 3: Thay ??i thông tin
1. **H? và tên** (b?t bu?c): Nh?p h? tên ??y ??
2. **Ch?c v?** (b?t bu?c): Ch?n t? dropdown
   - Admin (0)
   - Qu?n lý (1)
   - B?o v? (2)
   - K? thu?t (3)
   - Nhân viên (4)
3. **Gi?i tính**: Ch?n Nam/N?/Khác
4. **Ngày sinh**: Ch?n t? date picker
5. **S? ?i?n tho?i**: Nh?p 10-11 s?
6. **Ngày vào làm**: Ch?n t? date picker
7. **??a ch?**: Nh?p ??a ch? chi ti?t
8. **Tr?ng thái**: Toggle switch "?ang làm vi?c"

### B??c 4: L?u thay ??i
1. Click button **"L?u thay ??i"**
2. H? th?ng hi?n th? confirm dialog
3. Click **OK** ?? xác nh?n
4. Loading state hi?n th?
5. Thành công ? Hi?n th? thông báo ? Reload trang

---

## ?? Giao di?n Modal

### Edit Employee Modal
```
??????????????????????????????????????????????????
? [X] ?? Ch?nh s?a thông tin nhân viên          ?
??????????????????????????????????????????????????
?                                                ?
?  H? và tên *         Ch?c v? *                ?
?  [Nguy?n V?n A]      [? Qu?n lý]              ?
?                                                ?
?  Gi?i tính           Ngày sinh                 ?
?  [? Nam]             [?? 01/01/1990]           ?
?                                                ?
?  S? ?i?n tho?i       Ngày vào làm             ?
?  [0912345678]        [?? 01/01/2020]           ?
?                                                ?
?  ??a ch?                                       ?
?  [123 ???ng ABC, Qu?n 1, TP.HCM      ]        ?
?                                                ?
?  ? ?ang làm vi?c                               ?
?                                                ?
?  ?? L?u ý: Thay ??i ch?c v? ho?c tr?ng thái   ?
?  s? ?nh h??ng ??n quy?n truy c?p và phân ca   ?
?                                                ?
??????????????????????????????????????????????????
?              [H?y]  [?? L?u thay ??i]         ?
??????????????????????????????????????????????????
```

---

## ?? C?u trúc Code

### 1. Controller Action

**File:** `VehicleShiftController.cs`

```csharp
// API: C?p nh?t thông tin nhân viên
[HttpPost]
public async Task<IActionResult> UpdateEmployee([FromBody] UpdateEmployeeRequest request)
{
    try
    {
        var employee = await _context.NhanViens.FindAsync(request.MaNhanVien);
        if (employee == null)
        {
            return Json(new { success = false, message = "Không tìm th?y nhân viên" });
        }

        // C?p nh?t thông tin
        employee.HoTen = request.HoTen;
        employee.GioiTinh = request.GioiTinh;
        employee.SoDienThoai = request.SoDienThoai;
        employee.DiaChi = request.DiaChi;
        employee.ChucVu = request.ChucVu;
        employee.TrangThaiLamViec = request.TrangThaiLamViec;

        // Parse dates
        if (!string.IsNullOrEmpty(request.NgaySinh))
        {
            if (DateOnly.TryParse(request.NgaySinh, out var ngaySinh))
            {
                employee.NgaySinh = ngaySinh;
            }
        }

        if (!string.IsNullOrEmpty(request.NgayVaoLam))
        {
            if (DateOnly.TryParse(request.NgayVaoLam, out var ngayVaoLam))
            {
                employee.NgayVaoLam = ngayVaoLam;
            }
        }

        await _context.SaveChangesAsync();

        return Json(new { success = true, message = "C?p nh?t thông tin nhân viên thành công" });
    }
    catch (Exception ex)
    {
        return Json(new { success = false, message = "L?i: " + ex.Message });
    }
}
```

### 2. Request Model

```csharp
public class UpdateEmployeeRequest
{
    public int MaNhanVien { get; set; }
    public string HoTen { get; set; } = string.Empty;
    public string? GioiTinh { get; set; }
    public string? NgaySinh { get; set; }
    public string? SoDienThoai { get; set; }
    public string? DiaChi { get; set; }
    public int ChucVu { get; set; }
    public string? NgayVaoLam { get; set; }
    public bool TrangThaiLamViec { get; set; }
}
```

### 3. JavaScript Functions

**File:** `EmployeeList.cshtml`

#### A. Edit Function
```javascript
let currentEmployeeData = {};

function editEmployee() {
    if (!currentEmployeeData.employee) {
        alert('Vui lòng xem chi ti?t nhân viên tr??c khi ch?nh s?a!');
        return;
    }

    const emp = currentEmployeeData.employee;

    // ?óng detail modal và m? edit modal
    $('#employeeDetailModal').modal('hide');

    // Populate form v?i d? li?u hi?n t?i
    $('#editMaNhanVien').val(emp.maNhanVien);
    $('#editHoTen').val(emp.hoTen);
    $('#editChucVu').val(emp.chucVu);
    $('#editGioiTinh').val(emp.gioiTinh || '');
    $('#editSoDienThoai').val(emp.soDienThoai || '');
    $('#editDiaChi').val(emp.diaChi || '');
    $('#editTrangThaiLamViec').prop('checked', emp.trangThaiLamViec);

    // Format dates for input[type="date"]
    if (emp.ngaySinh) {
        const ngaySinh = new Date(emp.ngaySinh);
        $('#editNgaySinh').val(ngaySinh.toISOString().split('T')[0]);
    }

    if (emp.ngayVaoLam) {
        const ngayVaoLam = new Date(emp.ngayVaoLam);
        $('#editNgayVaoLam').val(ngayVaoLam.toISOString().split('T')[0]);
    }

    $('#editEmployeeModal').modal('show');
}
```

#### B. Save Function
```javascript
function saveEmployeeChanges() {
    // Validate form
    if (!$('#editEmployeeForm')[0].checkValidity()) {
        $('#editEmployeeForm')[0].reportValidity();
        return;
    }

    // Prepare data
    const formData = {
        MaNhanVien: parseInt($('#editMaNhanVien').val()),
        HoTen: $('#editHoTen').val().trim(),
        ChucVu: parseInt($('#editChucVu').val()),
        GioiTinh: $('#editGioiTinh').val() || null,
        SoDienThoai: $('#editSoDienThoai').val() || null,
        DiaChi: $('#editDiaChi').val() || null,
        NgaySinh: $('#editNgaySinh').val() || null,
        NgayVaoLam: $('#editNgayVaoLam').val() || null,
        TrangThaiLamViec: $('#editTrangThaiLamViec').is(':checked')
    };

    // Confirm before save
    if (!confirm('B?n có ch?c ch?n mu?n l?u thay ??i?')) {
        return;
    }

    // Show loading
    const saveBtn = $('button[onclick="saveEmployeeChanges()"]');
    const originalText = saveBtn.html();
    saveBtn.prop('disabled', true).html('<i class="fas fa-spinner fa-spin"></i> ?ang l?u...');

    // Send AJAX request
    $.ajax({
        url: '@Url.Action("UpdateEmployee", "VehicleShift", new { area = "Admin" })',
        type: 'POST',
        contentType: 'application/json',
        data: JSON.stringify(formData),
        success: function(response) {
            saveBtn.prop('disabled', false).html(originalText);
            
            if (response.success) {
                alert(response.message);
                $('#editEmployeeModal').modal('hide');
                location.reload();
            } else {
                alert('L?i: ' + response.message);
            }
        },
        error: function(xhr, status, error) {
            saveBtn.prop('disabled', false).html(originalText);
            alert('Có l?i x?y ra: ' + error);
        }
    });
}
```

---

## ?? Validation Rules

### 1. Required Fields
- ? **H? tên**: Không ???c ?? tr?ng
- ? **Ch?c v?**: Ph?i ch?n m?t giá tr?

### 2. Optional Fields
- ? Gi?i tính
- ? Ngày sinh
- ? S? ?i?n tho?i (pattern: 10-11 s?)
- ? Ngày vào làm
- ? ??a ch?

### 3. Business Rules
- S? ?i?n tho?i: 10-11 ký t? s?
- Ngày sinh: Không ???c l?n h?n ngày hi?n t?i
- Ngày vào làm: Không ???c l?n h?n ngày hi?n t?i
- Tr?ng thái: Boolean (checked/unchecked)

---

## ?? Flow Diagram

```
???????????????????????????????????????????????????
? 1. User clicks "Xem chi ti?t"                  ?
?    ? Load employee detail via AJAX             ?
?    ? Store data in currentEmployeeData         ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 2. User clicks "Ch?nh s?a"                     ?
?    ? Check if currentEmployeeData exists       ?
?    ? Close detail modal                        ?
?    ? Populate edit form with current data      ?
?    ? Show edit modal                           ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 3. User modifies fields                        ?
?    ? Validation on input                       ?
?    ? Real-time feedback                        ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 4. User clicks "L?u thay ??i"                  ?
?    ? Validate form (HTML5 + JS)               ?
?    ? Show confirmation dialog                  ?
?    ? Disable button + show loading             ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 5. Send AJAX POST request                      ?
?    ? URL: /Admin/VehicleShift/UpdateEmployee  ?
?    ? Content-Type: application/json            ?
?    ? Body: UpdateEmployeeRequest               ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 6. Controller processes request                ?
?    ? Find employee by MaNhanVien               ?
?    ? Update properties                         ?
?    ? Parse and validate dates                  ?
?    ? SaveChangesAsync()                        ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 7. Return JSON response                        ?
?    ? success: true/false                       ?
?    ? message: string                           ?
???????????????????????????????????????????????????
                  ?
                  ?
???????????????????????????????????????????????????
? 8. Handle response                             ?
?    ? Show alert with message                   ?
?    ? Close modal                               ?
?    ? Reload page to refresh data               ?
???????????????????????????????????????????????????
```

---

## ?? UI States

### 1. Normal State
- All fields enabled
- Button: "?? L?u thay ??i"

### 2. Loading State
- Button disabled
- Button text: "? ?ang l?u..."
- Spinner icon

### 3. Success State
- Alert: "? C?p nh?t thông tin nhân viên thành công"
- Modal closes
- Page reloads

### 4. Error State
- Alert: "? L?i: [error message]"
- Modal remains open
- User can retry

---

## ?? Data Flow

### Request
```json
POST /Admin/VehicleShift/UpdateEmployee
Content-Type: application/json

{
  "MaNhanVien": 1,
  "HoTen": "Nguy?n V?n A",
  "ChucVu": 1,
  "GioiTinh": "Nam",
  "NgaySinh": "1990-01-01",
  "SoDienThoai": "0912345678",
  "NgayVaoLam": "2020-01-01",
  "DiaChi": "123 ???ng ABC",
  "TrangThaiLamViec": true
}
```

### Response (Success)
```json
{
  "success": true,
  "message": "C?p nh?t thông tin nhân viên thành công"
}
```

### Response (Error)
```json
{
  "success": false,
  "message": "Không tìm th?y nhân viên"
}
```

---

## ?? Important Notes

### 1. Security
- ? S? d?ng `[FromBody]` ?? bind JSON
- ? Validate input trên server
- ?? TODO: Add authorization check
- ?? TODO: Log changes for audit

### 2. Data Integrity
- ? Validate dates before parsing
- ? Handle null values
- ? Use transactions (implicit in EF)

### 3. User Experience
- ? Confirm dialog before save
- ? Loading state during API call
- ? Clear success/error messages
- ? Auto reload after success

### 4. Performance
- ? Single database query to find
- ? Single SaveChangesAsync call
- ? No unnecessary joins

---

## ?? Common Issues & Solutions

### Issue 1: Modal không ?óng
**Cause:** Bootstrap modal conflict  
**Solution:** Use `$('#modalId').modal('hide')` instead of dismiss

### Issue 2: Date format không ?úng
**Cause:** JavaScript Date vs C# DateOnly  
**Solution:** Use `toISOString().split('T')[0]` ?? format

### Issue 3: Form validation không ho?t ??ng
**Cause:** Missing HTML5 attributes  
**Solution:** Add `required`, `pattern`, etc. và check `checkValidity()`

### Issue 4: Data không l?u
**Cause:** Missing `await _context.SaveChangesAsync()`  
**Solution:** ??m b?o g?i SaveChangesAsync()

---

## ?? Future Enhancements

### Phase 2:
1. ? **Upload avatar**: Thêm ch?c n?ng upload ?nh
2. ? **Change password**: Cho phép ??i m?t kh?u
3. ? **Activity log**: L?u l?ch s? thay ??i
4. ? **Bulk edit**: Ch?nh s?a nhi?u nhân viên cùng lúc
5. ? **Excel import**: Import danh sách t? Excel

### Phase 3:
1. ? **Permissions**: Role-based access control
2. ? **Approval workflow**: Yêu c?u phê duy?t khi thay ??i
3. ? **Email notification**: Thông báo khi thông tin thay ??i
4. ? **Version history**: Xem l?ch s? các phiên b?n

---

## ?? Testing Checklist

### Manual Testing:
- [ ] Hi?n th? form v?i d? li?u ?úng
- [ ] Validation required fields
- [ ] Validation phone number format
- [ ] Date picker ho?t ??ng
- [ ] Toggle switch ho?t ??ng
- [ ] Confirm dialog hi?n th?
- [ ] Loading state hi?n th?
- [ ] Success message hi?n th?
- [ ] Page reload sau khi l?u
- [ ] Error handling khi API fail
- [ ] Error handling khi không tìm th?y nhân viên

### Edge Cases:
- [ ] Employee không t?n t?i
- [ ] Ngày sinh l?n h?n ngày hi?n t?i
- [ ] S? ?i?n tho?i không h?p l?
- [ ] H? tên quá dài (>100 ký t?)
- [ ] Network timeout
- [ ] Concurrent edits

---

**Version**: 1.0  
**Last Updated**: 2024  
**Status**: ? Completed  
**Build**: ? Successful
